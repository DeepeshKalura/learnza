import 'package:flutter/material.dart';

class TeacherManagementPannel extends StatelessWidget {
  const TeacherManagementPannel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
