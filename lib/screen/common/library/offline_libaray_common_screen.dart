import 'package:flutter/material.dart';

class OfflineLibarayCommonScreen extends StatelessWidget {
  const OfflineLibarayCommonScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('OfflineLibarayCommonScreen'),
      ),
      body: const Center(
        child: Text('OfflineLibarayCommonScreen'),
      ),
    );
  }
}
